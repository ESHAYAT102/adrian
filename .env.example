ADRIAN_SESSION_SECRET=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Data directory for repositories, users, avatars, and metadata.
# Set this to a path OUTSIDE your project directory so data survives
# git pulls, rebases, and container rebuilds.
# Default: ../.adrian-data (parent of project root)
ADRIAN_DATA_DIR=

