services:
  adrian:
    build: .
    container_name: adrian
    environment:
      NEXT_PUBLIC_SITE_URL: http://localhost:8390
      ADRIAN_DATA_DIR: /data
    ports:
      - "8390:8390"
    volumes:
      - adrian-data:/data
    restart: unless-stopped

volumes:
  adrian-data:
