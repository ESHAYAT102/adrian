services:
  xenon:
    build: .
    container_name: xenon-selfhost
    environment:
      NEXT_PUBLIC_SITE_URL: http://localhost:3000
      XENON_DATA_DIR: /data
    ports:
      - "3000:3000"
    volumes:
      - xenon-data:/data
    restart: unless-stopped

volumes:
  xenon-data:
